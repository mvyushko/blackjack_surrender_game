def players_turn(player,dealer):

	hand_list = [player.hand]
	players_natural = []
	final_scores = []	

	while len(hand_list) > 0:
		hand = hand_list.pop(0)
	
		if hand.score > 0:
	
        		if hand.score < 21:
				players_natural.append(False)
	
				if hand.type == 'Normal':
					check for the split possibility
				
					if split possible and requested:
						split_card_1, split_card_2 = hand.split()
						player.split hand1.add_card_from_split(split_card_1)
						player.split hand2.add_card_from_split(split_card_2)
						split_hand_1.add_card_from_deck(deck)
						split_hand_2.add_card_from_deck(deck)

						hand.list.extend([player.split_hand1, player.split_hand2])

						dealer.hand.display_one_face_down()
						player.split_hand1.display_face_up()
						player.split_hand2.display_face_up()
						continue
					else:
						pass

                        	else:
					pass

				(check for double-down and hit cards until stay, bust, or 21)
				final_scores.append(hand.score)

			else:
				players_natural.append(True)

	return players.natural, final_scores 